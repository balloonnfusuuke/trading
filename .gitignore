# 秘密情報（これだけは絶対にコミットしない）
config.py
scheduler_state.json
.env
.env.*

# ログ・生成物
*.log
__pycache__/
*.pyc
.venv/
venv/

# スクリーニング CSV（コミットしたい場合はこのブロックを削除）
*_screening_*.csv
screening_result_*.csv

backup_v1/

# ── positions.json / pnl_log.csv について ─────────────────────
# GitHub Actions で「手元と同じ」Discord にするには、この2つをリポジトリに含める。
# 　→ リポジトリは必ず Private にし、ローカルで取引後に push すること。
# 含めたくない場合は下の2行のコメントを外して再び無視する。
# positions.json
# pnl_log.csv
